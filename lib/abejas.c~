#include "abejas.h"
#include <stdlib.h>

ABEJA* init_abeja(BEE_TYPE tipo, TABLERO* tablero)
{
  ABEJA *abeja = malloc(sizeof(ABEJA));
  abeja->tipo = tipo;
  //abeja->funcion
  abeja->solucion = tablero;
}
